import React from 'react'

function Button({title}) {
  return (
    <a href="#" className='inline-block px-6 py-3 flex-shrink-0 rounded-md text-lg text-background-accent bg-accent font-medium font-forma'>{title}</a>
  )
}

export default Button